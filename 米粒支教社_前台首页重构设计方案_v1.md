# 米粒支教社 · 前台首页重构设计方案

> 版本：v1.0  
> 日期：2026-08-06  
> 目标：将原有的多页路由前台改造为「明亮温暖的长滚动单页」，保留后台管理不变。

---

## 一、设计理念

### 1.1 核心意象

**「阳光从正午流淌到夕阳」**

- 首页是一个可以不断向下滑动的长页面，像一天中阳光的变化：从明亮的正午暖黄，过渡到清新的浅绿，最后沉入温暖的夕阳金黄。
- 整体氛围：**明亮、温暖、活泼、充满希望**，拒绝深色/科技/冷峻感。
- 色彩不是重点中的重点，但它是氛围的基底。

### 1.2 设计关键词

| 关键词 | 体现 |
|--------|------|
| 长滚动 | 单页应用，无路由跳转，滚动即导航 |
| 透明导航 | 顶部导航初始透明，滚动后变为毛玻璃 |
| 大留白 | Section 之间 80–120px 间距，呼吸感 |
| 活泼 | 大圆角、弥散阴影、渐变文字、浮动动画 |
| 温暖 | 暖绿 + 暖黄 + 米白，无冷色/深色 |

---

## 二、色彩系统

### 2.1 主色板

| Token | 色值 | 用途 | 情绪 |
|-------|------|------|------|
| `--color-primary` | `#6BAF92` | 按钮、链接、图标、强调文字 | 暖绿，生机 |
| `--color-primary-light` | `#7CB342` | hover 状态、渐变终点 | 更亮的绿 |
| `--color-accent` | `#F5A623` | 标签、CTA 点缀、夕阳区块 | 暖黄，活力 |
| `--color-accent-light` | `#F5D89A` | Hero 背景、渐变中间色 | 浅暖黄 |
| `--color-surface` | `#FAF9F6` | 页面背景、卡片底色 | 米白，温暖 |
| `--color-text` | `#2C3E33` | 主正文 | 深绿灰，柔和 |
| `--color-text-secondary` | `#6A7A6A` | 辅助文字 | 中绿灰 |
| `--color-text-muted` | `#9A9A8A` | 占位、禁用 | 暖灰 |
| `--color-border` | `#E8E6E1` | 分割线、边框 | 暖灰白 |

### 2.2 区块色彩流动

页面从上到下，背景色自然过渡，像阳光的变化：

```
Hero      →  #FFF8E7 → #F5E6C8 → #F5D89A   (正午暖黄，全屏沉浸)
About     →  #FAF9F6                           (明亮米白，清爽)
Videos    →  #FAF9F6 → #E8F5E9               (浅绿过渡，生机)
Materials →  #E8F5E9 → #C8E6C9               (暖绿沉淀，知识)
Gallery   →  #C8E6C9 → #6BAF92               (深绿艺术，沉浸)
Contact   →  #6BAF92 → #F5A623               (夕阳暖黄，号召)
Footer    →  #2C3E33                         (深绿收尾，稳重)
```

> 相邻区块之间使用 `linear-gradient(180deg, ...)` 做 120px 的柔和过渡带，避免色块硬切。

---

## 三、排版规范

### 3.1 字体栈

```css
font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto,
             'Helvetica Neue', Arial, 'Noto Sans SC', sans-serif;
```

### 3.2 字号层级

| 层级 | 尺寸 | 字重 | 行高 | 用途 |
|------|------|------|------|------|
| Display | `clamp(40px, 6vw, 72px)` | 800 | 1.1 | Hero 主标题 |
| H1 | `clamp(28px, 4vw, 48px)` | 700 | 1.2 | Section 标题 |
| H2 | `24px` | 600 | 1.3 | 卡片标题 |
| Body | `15px` | 400 | 1.8 | 正文 |
| Caption | `13px` | 400 | 1.5 | 辅助说明 |
| Label | `12px` | 600 | 1.4 | 标签、按钮 |

### 3.3 渐变文字

Hero 副标题和关键强调文字使用品牌渐变：

```css
background: linear-gradient(135deg, #6BAF92 0%, #F5A623 100%);
-webkit-background-clip: text;
-webkit-text-fill-color: transparent;
```

---

## 四、圆角与阴影

### 4.1 圆角体系

| 元素 | 圆角 | 说明 |
|------|------|------|
| 大容器 / Banner | `24px` | Hero 区域、大模块 |
| 卡片 / 按钮 | `16px` | 统一大圆角，友好感 |
| 输入框 / 小标签 | `12px` | 紧凑元素 |
| 图片 / iframe | `16px` | 与卡片一致 |
| 导航栏 | `0`（顶部固定） | 但内容区内部元素保持圆角 |

### 4.2 阴影体系

```css
--shadow-sm:   0 2px 8px rgba(107, 175, 146, 0.06);
--shadow-md:   0 8px 32px rgba(107, 175, 146, 0.08);
--shadow-lg:   0 12px 40px rgba(107, 175, 146, 0.12);
--shadow-glow: 0 0 24px rgba(107, 175, 146, 0.15);
```

> 阴影使用品牌色（暖绿）而非黑色，保持整体温暖氛围。

---

## 五、导航设计

### 5.1 透明导航栏（TransparentNav）

**位置**：固定顶部，`position: fixed`，全宽。

**初始状态**（页面顶部）：
- 背景：`transparent`
- 文字颜色：`#2C3E33`
- 无边框、无阴影

**滚动后状态**（scrollY > 60px）：
- 背景：`rgba(250, 249, 246, 0.75)`
- `backdrop-filter: blur(16px) saturate(1.2)`
- 底部边框：`1px solid rgba(107, 175, 146, 0.1)`
- 阴影：`0 4px 24px rgba(0,0,0,0.04)`
- 过渡动画：`all 0.4s ease`

**布局**：
- 左侧：Logo（🌱 米粒支教社）
- 右侧：导航链接 + CTA 按钮
- 链接：关于我们、示范课程、推普资料、儿童画展、联系我们
- 当前所在 Section 高亮：文字变 `#6BAF92` + 底部 2px 下划线

**交互**：
- 点击链接 → `scrollIntoView({ behavior: 'smooth' })` 平滑滚动到对应 Section
- CTA 按钮 → 滚动到 Contact Section

### 5.2 移动端适配

- 屏幕 < 768px 时，导航链接收起为汉堡菜单
- 菜单展开为全屏覆盖层，背景 `rgba(250, 249, 246, 0.95)` + `backdrop-filter: blur(20px)`

---

## 六、页面结构（长滚动单页）

首页由 6 个 Section + Footer 组成，全部在同一个页面内滚动。

```
┌─────────────────────────────────────┐
│  TransparentNav（固定顶部）            │
├─────────────────────────────────────┤
│  HeroSection        #FFF8E7 暖黄      │  100vh
├─────────────────────────────────────┤
│  AboutSection       #FAF9F6 米白      │  auto
├─────────────────────────────────────┤
│  VideosSection      #E8F5E9 浅绿      │  auto
├─────────────────────────────────────┤
│  MaterialsSection   #C8E6C9 暖绿      │  auto
├─────────────────────────────────────┤
│  GallerySection     #6BAF92 深绿      │  auto
├─────────────────────────────────────┤
│  ContactSection     #F5A623 夕阳      │  80vh
├─────────────────────────────────────┤
│  Footer             #2C3E33 深绿      │  auto
└─────────────────────────────────────┘
```

### 6.1 路由调整

原前台路由改为重定向到首页锚点：

| 原路由 | 新行为 |
|--------|--------|
| `/` | 长滚动首页 |
| `/about` | `<Navigate to="/#about" />` |
| `/videos` | `<Navigate to="/#videos" />` |
| `/materials` | `<Navigate to="/#materials" />` |
| `/gallery` | `<Navigate to="/#gallery" />` |
| `/contact` | `<Navigate to="/#contact" />` |

后台路由 `/admin/*` 保持不变。

---

## 七、各 Section 详细设计

### 7.1 HeroSection（正午阳光）

**背景**：`linear-gradient(160deg, #FFF8E7 0%, #F5E6C8 30%, #F5D89A 60%, #F5C76E 100%)`

**高度**：`min-height: 100vh`

**内容布局**：居中，垂直水平均居中

**元素**：
1. 装饰性光晕（绝对定位，`radial-gradient` 圆形，右上角，低透明度）
2. 主标题：「米粒支教社」`Display` 字号，深绿灰 `#2C3E33`
3. 副标题：「点亮乡村教育的微光」渐变文字（绿→黄）
4. 描述文字：15px，居中，最大宽度 520px，暖棕色 `#7a6a4a`
5. 双按钮：「了解我们」（主按钮，品牌绿）+ 「观看视频」（次按钮，半透明白底）
6. 底部渐变过渡带：`120px` 高，`transparent → #FAF9F6`

**响应式**：
- 移动端：标题缩小，按钮垂直排列，内边距增大

---

### 7.2 AboutSection（明亮米白）

**背景**：`#FAF9F6`

**内容**：
1. Section 标题：「我们的使命」`H1`
2. 描述段落：15px，行高 1.8，最大宽度 640px，居中
3. 数据统计行：3 列网格
   - 每个统计卡片：白底、圆角 16px、阴影 `shadow-sm`、居中数字 + 标签
   - 数字：28px，品牌绿，字重 800
   - 标签：13px，辅助灰
   - 数字入场动画：从 0 滚动递增到目标值（IntersectionObserver 触发）

---

### 7.3 VideosSection（浅绿生机）

**背景**：`linear-gradient(180deg, #FAF9F6 0%, #E8F5E9 100%)`

**内容**：
1. Section 标题：「示范课程」`H1`
2. 课程卡片网格：2 列（桌面）/ 1 列（移动端）
   - 每个卡片：白底、圆角 16px、阴影 `shadow-md`、hover 上浮 4px + `shadow-lg`
   - 顶部缩略图区：渐变色块（浅绿系）+ 播放图标
   - 标题：18px，字重 600
   - 描述：13px，辅助灰
   - 点击 → 弹窗播放视频（Modal + iframe）

---

### 7.4 MaterialsSection（暖绿沉淀）

**背景**：`linear-gradient(180deg, #E8F5E9 0%, #C8E6C9 100%)`

**内容**：
1. Section 标题：「推普资料」`H1`
2. 文件列表：纵向排列
   - 每个文件行：白底（半透明 `rgba(255,255,255,0.7)`）、圆角 12px、左侧图标 + 文件名 + 下载按钮
   - 图标：PDF 用暖红 `#E57373`，DOC 用暖蓝 `#64B5F6`
   - hover：背景变白，左侧边框出现 3px 品牌绿

---

### 7.5 GallerySection（深绿艺术）

**背景**：`linear-gradient(180deg, #C8E6C9 0%, #6BAF92 100%)`

**内容**：
1. Section 标题：「儿童画展」`H1`，白色文字 + 轻微文字阴影
2. 图片网格：3 列（桌面）/ 2 列（平板）/ 1 列（手机）
   - 每个图片：圆角 16px、aspect-ratio 1:1、object-fit cover
   - 图片懒加载：使用 `loading="lazy"` + 占位骨架
   - hover：图片放大 1.05 + 阴影 `shadow-glow`
   - 点击图片 → Lightbox 全屏查看

---

### 7.6 ContactSection（夕阳暖黄）

**背景**：`linear-gradient(180deg, #6BAF92 0%, #F5A623 60%, #F5C76E 100%)`

**高度**：`min-height: 80vh`

**内容**：
1. Section 标题：「加入我们」`H1`，白色文字
2. 描述：白色半透明文字
3. 联系表单：
   - 输入框：白底、圆角 12px、无边框、聚焦时品牌绿外发光
   - 提交按钮：白底、品牌黄文字 `#F5A623`、圆角 16px、阴影
4. 联系信息：邮箱、地址、社交媒体图标行

---

### 7.7 Footer

**背景**：`#2C3E33`

**内容**：
- 居中 Logo + 品牌语
- 快捷链接行
- 版权信息
- 文字颜色：`#8A9E8F`

---

## 八、交互规范

### 8.1 滚动渐入动画

每个 Section 内的元素使用统一的滚动触发动画：

```css
.reveal {
  opacity: 0;
  transform: translateY(40px);
  transition: all 0.8s cubic-bezier(0.4, 0, 0.2, 1);
}
.reveal.visible {
  opacity: 1;
  transform: translateY(0);
}
```

- 触发条件：`IntersectionObserver`，threshold 0.15
- 子元素交错延迟：`transition-delay: calc(var(--index) * 0.1s)`

### 8.2 卡片 Hover 效果

```css
card {
  transition: transform 0.4s ease, box-shadow 0.4s ease;
}
card:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow-lg);
}
```

### 8.3 数字递增动画

统计数字使用滚动触发，从 0 动画递增到目标值，持续 2s，ease-out。

### 8.4 导航锚点高亮

滚动时实时计算当前视口内的 Section，对应导航链接高亮。

---

## 九、技术实现要点

### 9.1 文件结构

```
src/
├── components/
│   ├── TransparentNav.tsx      # 透明导航栏
│   ├── Footer.tsx              # 页脚
│   ├── GlowCard.tsx            # 通用发光卡片
│   ├── RevealWrapper.tsx       # 滚动渐入包装器
│   └── CountUp.tsx             # 数字递增组件
├── sections/
│   ├── HeroSection.tsx
│   ├── AboutSection.tsx
│   ├── VideosSection.tsx
│   ├── MaterialsSection.tsx
│   ├── GallerySection.tsx
│   └── ContactSection.tsx
├── hooks/
│   ├── useScrollReveal.ts      # 滚动可见 Hook
│   └── useCountUp.ts           # 数字递增 Hook
├── styles/
│   └── global.css              # CSS 变量 + 动画关键帧
└── pages/
    └── Home.tsx                # 组装所有 Section
```

### 9.2 依赖

- `antd`：Button、Modal、Form、Input、Spin（已存在）
- `react-router-dom`：Navigate 重定向（已存在）
- 无需新增大型依赖

### 9.3 性能注意

- 图片懒加载 + 骨架屏
- IntersectionObserver 一次性触发（disconnect 后不再观察）
- 渐变背景使用 CSS 而非图片，减少资源加载
- 动画使用 `transform` 和 `opacity`，触发 GPU 加速

---

## 十、与原设计的对比

| 维度 | 原设计 | 新设计 |
|------|--------|--------|
| 页面结构 | 6 个独立页面，路由跳转 | 单页长滚动，锚点导航 |
| 导航栏 | 白色背景，sticky | 透明 → 毛玻璃，固定顶部 |
| 首页 | Banner + 课程预览 | 全屏 Hero + 6 个沉浸区块 |
| 色彩 | 默认绿 `#52c41a`，无体系 | 暖绿 `#6BAF92` + 暖黄 `#F5A623`，有流动 |
| 圆角 | 6~16px 混用 | 统一 16px（大容器 24px） |
| 阴影 | 黑色硬阴影 | 品牌色弥散阴影 |
| 动画 | 无 | 滚动渐入、数字递增、卡片悬浮 |
| 背景 | 纯白 `#f5f5f5` | 米白 → 浅绿 → 深绿 → 暖黄 渐变流动 |

---

## 十一、后续代码生成计划

1. `global.css` — CSS 变量 + 动画关键帧 + 工具类
2. `hooks/useScrollReveal.ts` — 滚动可见 Hook
3. `hooks/useCountUp.ts` — 数字递增 Hook
4. `components/TransparentNav.tsx` — 透明导航栏
5. `components/RevealWrapper.tsx` — 滚动渐入包装器
6. `components/CountUp.tsx` — 数字递增组件
7. `components/GlowCard.tsx` — 通用发光卡片
8. `sections/HeroSection.tsx` — 正午阳光首屏
9. `sections/AboutSection.tsx` — 使命 + 统计
10. `sections/VideosSection.tsx` — 课程卡片
11. `sections/MaterialsSection.tsx` — 资料列表
12. `sections/GallerySection.tsx` — 画展网格
13. `sections/ContactSection.tsx` — 夕阳联系区
14. `components/Footer.tsx` — 页脚
15. `pages/Home.tsx` — 组装首页
16. 路由调整 — 原页面重定向到锚点
