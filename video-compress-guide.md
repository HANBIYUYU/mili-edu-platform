# 视频压缩实操指南（上传前先压一版，又快又清晰）

> 适用：示范课 / 童声童语等教学视频。目标：把 4K/超高码率原片（常见 500MB–2GB）
> 压成 **H.264 + ≤1080p + 2~3Mbps** 的网页友好版，画质肉眼几乎无差。

## 推荐参数（为什么这样压）
- 编码：**H.264**（浏览器兼容最好，无需转码直接放 R2 播）
- 分辨率：**≤1080p**（1920×1080；竖屏手机素材用 1080×1920）
- 码率：**视频 2–3 Mbps**（讲课/板书/PPT 画面足够；动态多的活动记录可提到 4 Mbps）
- 帧率：25 或 30 即可（与素材一致）
- 音频：AAC 128kbps 立体声
- 质量观感换算：约等于 CRF 20–23 恒定质量输出

## 用 ffmpeg 一条命令
```bash
# 1080p 固定码率版（推荐，体积可控）
ffmpeg -i input.mp4 -c:v libx264 -preset medium -crf 22 \
  -maxrate 3M -bufsize 6M -vf "scale='min(1920,iw)':-2" \
  -c:a aac -b:a 128k -movflags +faststart output.mp4

# 超长视频控制体积用两遍码率：-b:v 2.5M -maxrate 2.5M -bufsize 5M（替代上面 -crf）
```

### 估体积（2.5Mbps 视频 + 128k 音频 ≈ 0.33 MB/秒）
| 时长 | 预计体积 |
|---|---|
| 5 分钟 | ~95–110 MB |
| 10 分钟 | ~190–220 MB |
| 30 分钟 | ~550–650 MB（建议拆 2–3 集，单集 ≤300MB） |

## 不用命令行（图形工具）
- **剪映（免费）**：导出设置选 → 自定义 → 分辨率 1080p、码率 中/较高(约 3Mbps)、编码 H.264、格式 MP4
- **格式工厂 / HandBrake（免费）**：预设选 `H.264 + MP4 + 1080p`，质量档 CRF 20–22
- **手机拍摄后**：微信/QQ“发给自己→原图”不压缩视频；iPhone 拍摄设置里可开“高效(HEVC)”，上传前转 H.264 更稳

## 注意事项
- 上传到网站的视频请用 **MP4(H.264)**；WebM 也可以，MOV(Apple) 播放兼容差，先转 MP4
- 建议单条视频 **≤30 分钟、≤500MB**：上传快、观众打开快、拖动进度条也流畅
- 保存原片留档，只把压缩版上传到网站

## 大文件（>90MB）现在怎么传
无需压缩也能传：网站已支持 **分片直传 R2（每片 64MB，3 路并发）**，浏览器直接 PUT 到
R2 存储端点，不再受 Worker 100MB 限制；上传弹窗会显示百分比 + “第 x/N 片”。
> 仍建议先按本指南压一版——存储便宜 + 观众体验好。
